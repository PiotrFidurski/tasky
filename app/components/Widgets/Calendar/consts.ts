/**
 * Sizes for calendar to calculate min height.
 *
 * @remarks numbers represent `rem` values eg 4rem --> 64px
 */
const ROW_SIZE = 3;
const HEADER_SIZE = 5;
const POSTION_TOP = 4;

export { HEADER_SIZE, POSTION_TOP, ROW_SIZE };
