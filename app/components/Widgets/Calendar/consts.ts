/**
 * Sizes for calendar to calculate min height.
 *
 * @remarks numbers represent `rem` values eg 4rem --> 64px
 */
const HEADER_SIZE = 5;
const ROW_SIZE = 3;
const ROWS_POSTION_TOP = 4;

export { HEADER_SIZE, ROWS_POSTION_TOP, ROW_SIZE };
